import { NavigationContainer } from '@react-navigation/native';
import Routing from './navigation';


const App = () => {
  return (
    <NavigationContainer>
      <Routing />
    </NavigationContainer>
  );
};

export default App;
